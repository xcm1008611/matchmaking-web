# 同心锁抬升高度修正操作指南

## 目标
修正 0-20 帧锁梁Z轴抬升高度过大的问题，使其仅略微超出锁体表面即可。

## 操作步骤

### 1. 打开文件
- 启动 Blender
- 打开文件：`c:\Users\yin\Desktop\工作\红娘网\concentric_lock_source.blend`

### 2. 检查当前状态
- 在时间轴中，将播放头拖到 **第0帧**
- 查看锁梁的初始位置（开锁悬浮状态）
- 按 **空格键** 播放动画，观察 0-20 帧的抬升幅度

### 3. 选择锁梁物体
- 在 **大纲视图（Outliner）** 或 **3D视图** 中点击选中锁梁（U型部件）
- 确保物体被选中（轮廓高亮显示）

### 4. 修正第20帧的高度

#### 方法A：直接调整（推荐）
1. 将时间轴播放头移到 **第20帧**
2. 按 **G** 键（移动工具），然后按 **Z** 键（限制在Z轴）
3. 向下拖动鼠标，降低锁梁高度
4. **目标高度**：短杆底部刚好超过锁体表面 1-2厘米即可
   - 观察侧视图，确保长杆底部仍在锁孔附近
   - 不要抬得太高，只要旋转时不会穿模即可
5. 点击鼠标左键确认位置
6. 按 **I** 键，选择 **位置（Location）** 插入关键帧

#### 方法B：精确数值调整
1. 将时间轴播放头移到 **第20帧**
2. 选中锁梁后，查看右侧 **属性面板 > 变换（Transform）**
3. 在 **位置 Z** 值中：
   - 当前值可能是 `0.8` 或更高
   - 将其降低到 `0.15` 到 `0.25` 之间（根据锁体高度调整）
4. 在 **位置 Z** 输入框上 **右键 > 插入关键帧**

### 5. 验证修正效果
1. 将播放头移回 **第0帧**
2. 按 **空格键** 播放动画
3. 观察 0-20 帧：
   - ✅ 锁梁应该缓缓抬起，幅度较小
   - ✅ 短杆底部略微超过锁体表面
   - ✅ 长杆底部仍在锁孔区域附近
   - ✅ 20-50 帧旋转时不会穿模
4. 从多个视角（前视图F1、侧视图F3、俯视图F7）检查

### 6. 保存文件
- 按 **Ctrl + S** 保存当前 .blend 文件
- 或 **文件 > 另存为** 保存到：
  ```
  c:\Users\yin\Desktop\工作\红娘网\concentric_lock_fixed.blend
  ```

### 7. 导出GLB文件
1. **文件 > 导出 > glTF 2.0 (.glb/.gltf)**
2. 导出设置：
   - ✅ **格式**：GLB（二进制）
   - ✅ **包含** > **动画**：勾选
   - ❌ **合并网格**：取消勾选（重要！）
   - ✅ **应用修改器**：勾选
   - ✅ **+Y 向上**：勾选
3. 文件名：`concentric_lock_fixed.glb`
4. 保存位置：`c:\Users\yin\Desktop\工作\红娘网\`
5. 点击 **导出 glTF 2.0**

## 关键检查点

### ✅ 必须确认的项目
- [ ] 0-20帧抬升高度合理（短杆底部略超锁面）
- [ ] 长杆底部仍在锁孔附近，没有完全脱离
- [ ] 20-50帧旋转时无穿模
- [ ] 50-70帧下落扣入锁孔顺畅
- [ ] 动画完整70帧，关键帧在0/20/50/70
- [ ] 两个物体独立（锁体、锁梁未合并）
- [ ] GLB文件包含动画数据

## 参考数值（根据实际模型调整）

假设锁体高度约为 `0.8` 单位：
- **第0帧**：Z = 0.5（开锁悬浮）
- **第20帧修正后**：Z = 0.65（仅抬升0.15，约为锁体高度的18%）
- **第50帧**：Z = 0.65（保持旋转高度）
- **第70帧**：Z = 0.4（下落扣锁）

## 如果遇到问题

### 问题1：找不到锁梁物体
- 在大纲视图中查找名称包含 "shackle"、"beam"、"梁" 的物体
- 或在3D视图中点击U型部件

### 问题2：修改后关键帧丢失
- 确保在修改后按 **I** 键插入关键帧
- 或在属性面板的数值上右键 > 插入关键帧

### 问题3：旋转时穿模
- 需要进一步降低第20帧和第50帧的Z高度
- 或稍微调整旋转角度（减少到25°）

### 问题4：导出的GLB没有动画
- 确保导出设置中勾选了 **动画**
- 确认时间轴中存在关键帧（蓝色菱形标记）

## 完成后
将修正后的文件发给我确认：
- `concentric_lock_fixed.blend`
- `concentric_lock_fixed.glb`

我会帮你验证动画是否符合要求。
